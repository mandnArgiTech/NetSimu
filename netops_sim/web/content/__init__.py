"""Educational content served by the visual lab as Markdown.

Each concept lives at content/concepts/<id>.md with optional YAML-ish front
matter (just a `title:` line for now). Editing these files does NOT require
a recompile — the backend re-reads on each request.
"""
